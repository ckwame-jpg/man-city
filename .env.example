# Public URL of the site (used for metadataBase and OG/Twitter images)
# For local development, leave as http://localhost:3000
# For Vercel, set to your production URL (e.g., https://chrisprem.xyz)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: URL to the live demo of the Fantasy app for the portfolio button
NEXT_PUBLIC_FANTASY_DEMO_URL=

# GitHub personal access token (classic) with read:user scope
# Used for the contribution heatmap on the portfolio page
GITHUB_TOKEN=
